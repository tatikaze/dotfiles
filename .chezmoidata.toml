# chezmoi static data — マージ先はテンプレート変数の root namespace
# 個人情報 (name/email/work) は .chezmoi.toml.tmpl から `chezmoi init` 時にプロンプト取得する

[darwin]
    homebrew_prefix = "/opt/homebrew"

[linux]
    homebrew_prefix = "/home/linuxbrew/.linuxbrew"
